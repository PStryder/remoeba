"""Remote model inference: the wire format, the transports, and the service.

`wire` is pure and importable anywhere. `transport` and `service` belong to
the inference service process, the only one that ever holds the provider
credential.
"""
